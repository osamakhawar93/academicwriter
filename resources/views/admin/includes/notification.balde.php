            <!-- ========== Notification Start ========== -->

            @if(Session::get('success'))
            
            @endif
            
            @if(Session::get('error'))
            
            @endif
            
            @if(Session::get('warning'))
            
            @endif
            
            @if(Session::get('info'))
            
            @endif
            <!-- Notification End -->